# chat/llm_stub.py
def fallback_reply(user_text: str):
    # Simple, friendly fallback reply to keep UX pleasant
    return "Sorry — I didn't get that. Try 'demo' or type a loan amount like 'I want ₹300000'."
